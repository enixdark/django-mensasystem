# import time
# import threading
import datetime
from models import *
from django.contrib.auth.models import User

# current_time = date.today()
# check = False

# def system_check():
#     # global current_time,check
#     # if not check or current_time + datetime.timedelta(days=1) < date.today():
#     for user in User.objects.all():
#         try:
#             # people = UserTimeSheet.objects.getTimeSheetForUser()
#             # people = UserTimeSheet().get_all()
#         except:
            # people = UserTimeSheet().get_all()
            # people.save()

        # current_time = date.today()
        # check = True


# class SystemThread(threading.Thread):
#     def __init__(self):
#         threading.Thread.__init__(self)
#         self.keeprunning = True
#         self.state = 0
#         self.interrupt = threading.Event()
#
#     def run(self):
#         # this loop will run until you call set() on the interrupt
#         while not self.interrupt.isSet():
#             self.state = max(self.state,system_check())
#             # this will either sleep for 10 seconds (for the timeout)
#             # or it will be interrupted by the interrupt being set
#             # self.interrupt.wait(120)



